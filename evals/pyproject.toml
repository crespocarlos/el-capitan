[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "el-capitan-evals"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.25.0",
    "tiktoken>=0.7.0",
]
