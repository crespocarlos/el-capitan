---
description: "TypeScript style guide — load this rule when working in TypeScript repos. Not auto-applied."
globs:
alwaysApply: false
---

# Style Preferences

- TypeScript, const arrow functions, explicit return types
- snake_case filenames, PascalCase components
- No `any`, no `@ts-ignore`
- Single quotes
- `import type` for type-only imports
